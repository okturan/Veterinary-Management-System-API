VET_DB_URL=jdbc:postgresql://localhost:5432/veterinary_management
VET_DB_USER=veterinary_app
VET_DB_PASSWORD=
# Schema changes are opt-in. Use create only for a disposable empty local database.
VET_DDL_AUTO=validate
# Synthetic demo rows are disabled unless explicitly enabled.
VET_SEED_DEMO_DATA=false
