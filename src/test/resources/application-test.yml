spring:
  datasource:
    url: jdbc:h2:mem:veterinary;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password: ""
    driverClassName: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop

vet:
  seed-demo-data: false
